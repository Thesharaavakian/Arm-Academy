# ⚠️  Run deploy.sh — it generates this file with real values.
# Never commit this file with real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: arm-academy-secrets
  namespace: arm-academy
type: Opaque
stringData:
  SECRET_KEY: "__REPLACE_SECRET_KEY__"
  DATABASE_URL: "postgresql://arm_user:__REPLACE_DB_PASS__@postgres-svc:5432/arm_academy"
  REDIS_URL: "redis://redis-svc:6379/0"
  POSTGRES_PASSWORD: "__REPLACE_DB_PASS__"
  EMAIL_HOST_PASSWORD: "__REPLACE_EMAIL_HOST_PASSWORD__"
  EMAIL_HOST_USER: "__REPLACE_EMAIL_HOST_USER__"
  ZADARMA_API_KEY: "__REPLACE_ZADARMA_API_KEY__"
  ZADARMA_API_SECRET: "__REPLACE_ZADARMA_API_SECRET__"
  STRIPE_SECRET_KEY: "__REPLACE_STRIPE_KEY__"
  STRIPE_WEBHOOK_SECRET: "__REPLACE_STRIPE_WEBHOOK__"
