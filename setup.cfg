[flake8]
max-line-length = 130
exclude = migrations,venv,.venv,__pycache__,node_modules
extend-ignore =
    # E221: multiple spaces before operator — intentional column-aligned assignments
    E221,
    # W503: line break before binary operator — conflicts with Black
    W503
